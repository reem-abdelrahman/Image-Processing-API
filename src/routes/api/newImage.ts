import express from 'express';   
const newImage = express.Router();
newImage.get('/', async (req, res) => { 
 //const originalImageExists= await checkOriginalImageExists()

});

export default newImage;